Format the csv output from the canvas gradebook export.

Add a student name to the delete_names list to remove that row.

Run `python main.py input_file_name.csv` to produce a formated `output.csv` file.